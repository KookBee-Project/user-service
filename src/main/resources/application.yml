server:
  port: 9000

spring:
  datasource:
    url: jdbc:mysql://192.168.80.214:3306/kookbee?serverTimezone=Asia/Seoul
    username: bee
    password: 1q2w3e4r
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100
  application:
    name: user-service

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://192.168.80.214:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
    # ???? ???? ???? ????? ??

